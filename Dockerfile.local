FROM node:22-slim

WORKDIR /app

# Copy the pre-packaged standalone production build generated on the host
COPY deploy-api ./

# Expose API port
EXPOSE 4000

ENV PORT=4000
ENV NODE_ENV=production

# Run using tsx
CMD ["npx", "tsx", "src/server.ts"]
